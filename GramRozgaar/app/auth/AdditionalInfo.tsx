import Info from "@/components/auth/AddditinalInfo";
import { View } from 'react-native';

const AdditionalInfo = () => {
    return (
        <View>
            <Info />
        </View>
    );
};

export default AdditionalInfo;

export const options = {
    headerShown: false,
};
